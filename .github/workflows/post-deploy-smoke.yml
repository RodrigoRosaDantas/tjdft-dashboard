name: Smoke do GitHub Pages publicado

on:
  workflow_run:
    workflows: ["Deploy TJDFT dashboard to GitHub Pages"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Testar rotas publicadas
        shell: bash
        run: |
          set -euo pipefail
          base="https://rodrigorosadantas.github.io/tjdft-dashboard"
          routes=(
            "/"
            "/hoje/"
            "/mentor/"
            "/trilha/"
            "/agenda/"
            "/revisoes/"
            "/erros/"
            "/desempenho/"
            "/riscos/"
            "/tecnico/"
            "/analista/"
            "/qualidade-dados/"
            "/painel-legado/"
            "/portugues-rlm/"
            "/portugues-rlm/p01/"
            "/portugues-rlm/rl01/"
            "/portugues-rlm/rev01/"
            "/portugues-rlm/flashcards/"
            "/leis/"
            "/leis/l01/"
            "/leis/flashcards/"
            "/sincronizacao/"
            "/manifest.webmanifest"
            "/sw.js"
            "/data/tjdft-snapshot.json"
            "/data/tjdft-edital.json"
            "/data/portugues-rlm.json"
            "/data/leis-primeiro.json"
          )
          for route in "${routes[@]}"; do
            echo "SMOKE $route"
            curl --fail --silent --show-error --location --retry 4 --retry-delay 2 --connect-timeout 10 --max-time 30 "$base$route" -o /tmp/smoke
            test -s /tmp/smoke
          done

      - name: Validar marcadores do produto publicado
        shell: bash
        run: |
          set -euo pipefail
          base="https://rodrigorosadantas.github.io/tjdft-dashboard"
          check_marker() {
            local route="$1"
            local marker="$2"
            curl -fsSL --retry 4 --connect-timeout 10 --max-time 30 "$base$route" -o /tmp/marker
            grep -qi "$marker" /tmp/marker
          }
          check_marker "/" "Central de comando"
          check_marker "/hoje/" "Executar agora"
          check_marker "/mentor/" "Por que esta decisão"
          check_marker "/desempenho/" "ausência"
          check_marker "/leis/" "Leis Primeiro"
          check_marker "/portugues-rlm/" "Português Primeiro"
          check_marker "/manifest.webmanifest" '"display"'
          check_marker "/sw.js" "tjdft-pages-v3"
          check_marker "/data/tjdft-snapshot.json" '"kind": "notion"'
          check_marker "/data/portugues-rlm.json" '"P01"'
          check_marker "/data/leis-primeiro.json" '"L01"'

      - name: Validar fallback publicado
        shell: bash
        run: |
          set -euo pipefail
          base="https://rodrigorosadantas.github.io/tjdft-dashboard"
          status=$(curl --silent --show-error --location --output /tmp/fallback --write-out "%{http_code}" "$base/__smoke-route-que-nao-existe__")
          test "$status" = "404"
          test -s /tmp/fallback
